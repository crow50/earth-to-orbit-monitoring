# Smoke Test Verification Report

- **Project**: earth-to-orbit-monitoring
- **Timestamp (UTC)**: 2026-03-03T20:36:46Z
- **Branch/Commit**: `main` / `1b73a7b02fcb8f2103ae6d7609386ea1f6ed31bc`

## Summary
- **Hygiene**: **PASS**
- **Build**: **PASS** (Docker images/services built and started)
- **Unit Tests**: **FAIL** (**3 passed / 4 total**, 1 failed)
- **Functional Check**: **PASS** (API and frontend HTTP 200)

## Section-by-Section (Smoke Test Playbook v1.0)

### 1) Environment Verification (Pre-Flight)
- Ran repo hygiene gate:
  - Command: `/root/.openclaw/scripts/repo_hygiene_gate.sh /root/.openclaw/shared/projects/earth-to-orbit-monitoring`
  - Result: `SUCCESS: Repo is clean and synced.`
- Dependency versions verified:
  - `python --version`: not found
  - `python3 --version`: `Python 3.12.3`
  - `node --version`: `v22.22.0`
  - `docker compose version`: `v2.22.0`

### 2) Dependency Management
- `make test` created and used Python venv at:
  - `services/poller/.venv`
- Test dependencies installed via:
  - `pip install -e .[test]`

### 3) Database & State Initialization
- Clean state established with:
  - `docker compose down -v`
- Fresh stack started:
  - `docker compose up -d --build`

### 4) Test Suite Execution
- Command: `make test`
- Result: **FAILED** (`exit 2`)
- Output summary:
  - `1 failed, 3 passed in 0.35s`
  - Failing test: `tests/test_poller.py::test_fetch_launches_pagination`

### 5) Functional "Happy Path" Check
- Services brought up successfully (`docker compose ps` showed all core services Up).
- HTTP checks:
  - `curl -f http://localhost:8000` → 200 (after immediate retry)
  - `curl -f http://localhost:3000` → 200

## Exit Codes (Major Commands)
- `repo_hygiene_gate.sh`: `0`
- `make test`: `2`
- `docker compose down -v && up -d --build`: `0`
- Functional HTTP checks (final state): `0`

## Critical Logs
```text
FAILED tests/test_poller.py::test_fetch_launches_pagination - poller.client.LaunchLibraryError
Request:
- GET https://example.com/launches/?limit=100&mode=detailed
Available matches:
- GET https://example.com/launch/ URL does not match
- GET https://example.com/launch/?page=2 URL does not match
```

## Escalation
- Blocker reported to EJ via Telegram per protocol.

